package day16_reviewsession1;

import java.util.Scanner;

public class VariablesAndTypes {
	public static void main(String[] args) {
// variables -> data container.
		/*
		 * Important things about using variables in Japes -> type/datatype, name value
		 * 
		 * Java is a strongly types language: -> it means before using any variable, we
		 * need to declare first When you declare, we need to put type and name int
		 * steps; int steps;// will not let to declare some variable twice double steps;
		 * // not allowed Variable name - sometimes can be called as Identifiers
		 * 
		 * double price; System.out.println(price); // not initialized compile error
		 * ------------------------- Initilize - means vale for the first time.
		 * 
		 * double price = 12.40;
		 */

		// Variable naming rules:

		// 1) can be any length -no limit
		    String adsafsdfdfdfgdf;

		// 2) can contain letters, _ , $, numbers.
		    int today_is_review1_$session = 1;

		// 3) can not start with numbers. Can contain
		// int 1stDay- will not work;

		// 4) No reserved word, Java keywords allowed.
		// int public; int double; int class;
		// but can use it with upper case
		// Class, Public, Double
		    
		    
		    // meaningful names are preferred. and start with lowercase, then next word
		    // start with upper case. We call the style camelStyle.
		    
		    long wordPopulationCount;
		    
		    int Class;// Java is case sensetive. Class is not class.
		    int Int = 123;
		    Scanner scanner=new Scanner(System.in);
		    scanner.next();
	}
}
